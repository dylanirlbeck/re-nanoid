{
  "name": "re-nanoid",
  "version": "0.2.1",
  "esy": {
    "build": "dune build -p #{self.name}"
  },
  "dependencies": {
    "@opam/dune": "1.11.4",
    "refmterr": "*"
  },
  "devDependencies": {
    "ocaml": "4.6.1000",
    "@opam/merlin": "*",
    "@opam/dune-release": "*"
  },
  "scripts": {
    "watch": "esy dune build @all -p #{self.name} --watch"
  }
}
